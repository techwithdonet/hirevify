// TESTING MODE CONFIGURATION
// Set TESTING_MODE to true to bypass all premium gates and enable full access
// Set TESTING_MODE to false to restore normal premium access controls
export const TESTING_MODE = true;

// Log testing mode status
if (TESTING_MODE) {
  console.log('🧪 HireVify Testing Mode: Premium features are unlocked');
}